# Core training stack
torch==2.7.0
transformers==4.57.6
accelerate==1.6.0
trl==0.24.0
datasets==3.5.0
peft==0.15.1
bitsandbytes
vllm>=0.8.5,<0.9
wandb

# Online SDPO training/generation (Tinker-hosted; requires TINKER_API_KEY)
tinker

# User simulator / judge (DeepSeek, via its Anthropic-compatible endpoint; requires DEEPSEEK_API_KEY)
anthropic
